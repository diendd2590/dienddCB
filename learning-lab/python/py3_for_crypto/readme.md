Đây là dir luyện viết mấy code python để decode và endcode mục tiêu của tôi là một code tự detect 
và giải mã người dùng nhập vào có thể tùy chọn giải chồng nếu chưa ra được sâu mong muốn

Đây cx là dir đầu tiên luyện code python nếu code ko đẹp lắm xin thông cảm :( !
